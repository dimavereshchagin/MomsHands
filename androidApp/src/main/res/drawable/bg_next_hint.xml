<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/next_hint_background" />
    <corners android:radius="@dimen/next_hint_corner" />
    <stroke
        android:width="1dp"
        android:color="@color/divider" />
</shape>